# URL of the proxchat signaling server (used by webpack DefinePlugin at build time).
# WebSocket URL is derived automatically (https:// → wss://).
# Defaults to https://proxchat.dant123.com when unset.
PROXCHAT_SERVER=https://proxchat.your-domain.com
